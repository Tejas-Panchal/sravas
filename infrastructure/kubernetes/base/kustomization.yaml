apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sravas

resources:
  - namespace.yaml
  - ingress-class.yaml
  - mongodb/
  - elasticsearch/
  - kafka/
# Day 3: core service deployments
# Day 4: remaining service deployments + frontend
# Day 5: HPA
