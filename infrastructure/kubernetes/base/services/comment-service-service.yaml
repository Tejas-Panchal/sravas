apiVersion: v1
kind: Service
metadata:
  name: comment-service
  labels:
    app: comment-service
spec:
  type: ClusterIP
  selector:
    app: comment-service
  ports:
    - port: 3006
      name: http
