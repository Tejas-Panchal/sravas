apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduler-service
  labels:
    app: scheduler-service
    app.kubernetes.io/component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scheduler-service
  template:
    metadata:
      labels:
        app: scheduler-service
    spec:
      containers:
        - name: scheduler-service
          image: sravas-scheduler-service
          ports:
            - containerPort: 3009
              name: http
          envFrom:
            - configMapRef:
                name: scheduler-service-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: sravas-secrets
                  key: database-url
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 3009
            initialDelaySeconds: 5
            periodSeconds: 5
