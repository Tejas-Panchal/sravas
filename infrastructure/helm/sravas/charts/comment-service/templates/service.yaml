apiVersion: v1
kind: Service
metadata:
  name: {{ include "sravas.fullname" . }}
  labels:
    {{- include "sravas.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "sravas.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      name: http
